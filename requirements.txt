# Python dependencies for the Overload-Safe Queue API
#
# Install with: pip install -r requirements.txt

# Web Framework
fastapi>=0.104.0

# ASGI Server
uvicorn[standard]>=0.24.0

# Async HTTP Client (for Upstash REST API)
httpx>=0.25.0

# Data Validation (included with FastAPI, but explicit)
pydantic>=2.5.0
